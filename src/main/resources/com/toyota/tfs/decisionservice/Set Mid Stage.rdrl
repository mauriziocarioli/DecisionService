package com.toyota.tfs.decisionservice;

import java.lang.Number;

rule "Set Mid Stage"
	dialect "mvel"
	when
		AccountHolder( daysDelinquent >= 46 && daysDelinquent <= 89 )
	then
		insert( new MidStageIsSet() );
end
